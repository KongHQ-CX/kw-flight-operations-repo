---
_format_version: '3.0'
add-plugins:
- selectors:
  - "$..routes[*]"
  overwrite: false
  plugins:
    - name: correlation-id
      config:
        header_name: Routes-Request-ID
        generator: uuid#counter
        echo_downstream: false